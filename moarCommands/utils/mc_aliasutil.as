/*namespace mc
{
	string[][] aliases;

	void putAliasFor(string toalias, string aliasmod)
	{
		string[] d = {toalias, aliasmod};
		aliases.push_back(d);
	}

	string parseAlias(string input)
	{
		int findpos;
		for (int i = 0; i < aliases.size(); i++)
		{
			string[] args;
			for (int word = 0; j < )
		}

		return input;
	}
}*/